# Notes for Redux & React-Redux 

- Redux is a core library, react-redux is its implementation
- redux-toolkit automatically `"never mutates your state"`.
- Each application can only have one store (Single source of truth).